# Исправление проблемы с моделями

## ❌ Проблема

Модель `cointegrated/rut5-base-summarizer` не найдена или недоступна.

## ✅ Решение

Используйте альтернативные модели для суммаризации:

### Рекомендуемые модели суммаризации:

1. **IlyaGusev/rut5_base_sum_gazeta** (основная рекомендация)
   - ✅ Доступна и работает
   - ✅ Отличное качество для русского языка
   - ✅ Специализирована для суммаризации

2. **IlyaGusev/rut5_base_sum_gazeta_v2** (альтернатива)
   - ✅ Улучшенная версия
   - ✅ Также отличное качество

3. **cointegrated/rut5-base** (резервная)
   - ✅ Более общая модель
   - ✅ Можно использовать для суммаризации

## 🚀 Быстрое исправление

Скрипты уже обновлены и автоматически попробуют все варианты моделей.

Просто запустите заново:

```bash
python download_models_recommended.py
```

Или скачайте модель вручную:

```bash
# Установите huggingface-cli если еще не установлен
pip install huggingface_hub

# Скачайте модель
huggingface-cli download IlyaGusev/rut5_base_sum_gazeta --local-dir models/summarizer
```

## 📝 Что изменилось

1. ✅ Скрипты обновлены с правильными именами моделей
2. ✅ Добавлен автоматический fallback на альтернативные модели
3. ✅ Код analyzer-service обновлен для работы с новыми моделями

## 🔍 Проверка

После скачивания проверьте:

```bash
ls -la models/summarizer/
```

Должны быть файлы:
- config.json
- pytorch_model.bin (или model.safetensors)
- tokenizer_config.json
- и другие файлы модели

## 💡 Если все еще не работает

1. Проверьте интернет-соединение
2. Попробуйте скачать модель напрямую через браузер: https://huggingface.co/IlyaGusev/rut5_base_sum_gazeta
3. Используйте другую модель из списка альтернатив

