# Market Analytics - Android App

Мобильное приложение для парсинга и анализа отзывов с маркетплейсов.

## 📱 Функциональность

- **Просмотр товаров** - список всех добавленных товаров для мониторинга
- **Добавление товаров** - добавление новых товаров по URL с автоопределением маркетплейса
- **Парсинг отзывов** - запуск парсинга отзывов с маркетплейсов
- **Просмотр отзывов** - детальный просмотр отзывов с рейтингами
- **Настройки** - настройка URL API сервера и ID пользователя

## 🛒 Поддерживаемые маркетплейсы

- Wildberries
- Ozon
- Яндекс.Маркет

## 🔧 Требования

- Android Studio Hedgehog (2023.1.1) или новее
- JDK 17
- Android SDK 34
- Минимальная версия Android: 8.0 (API 26)

## 🚀 Сборка и запуск

### 1. Откройте проект в Android Studio

```
File -> Open -> выберите папку android-app
```

### 2. Синхронизируйте Gradle

Android Studio автоматически предложит синхронизацию. Нажмите "Sync Now".

### 3. Настройте подключение к серверу

По умолчанию приложение настроено на подключение к:
- **Эмулятор**: `http://10.0.2.2:8002` (localhost хост-машины)
- **Реальное устройство**: нужно указать IP компьютера в настройках приложения

### 4. Запустите приложение

- Выберите эмулятор или подключенное устройство
- Нажмите "Run" (зелёная кнопка Play)

## 🌐 Настройка API

### Для эмулятора

1. Запустите Docker-контейнеры на компьютере:
```bash
docker-compose up -d
```

2. Приложение автоматически подключится к `http://10.0.2.2:8002`

### Для реального устройства

1. Узнайте IP-адрес компьютера в локальной сети:
```powershell
ipconfig
```

2. Убедитесь, что телефон и компьютер в одной сети

3. В приложении откройте Настройки и укажите URL:
```
http://192.168.X.X:8002
```
(где 192.168.X.X — IP вашего компьютера)

## 📂 Структура проекта

```
android-app/
├── app/
│   ├── src/main/
│   │   ├── java/com/marketanalytics/app/
│   │   │   ├── data/
│   │   │   │   ├── api/          # Retrofit API клиент
│   │   │   │   ├── model/        # Модели данных
│   │   │   │   └── repository/   # Репозитории
│   │   │   ├── ui/
│   │   │   │   ├── components/   # Переиспользуемые UI компоненты
│   │   │   │   ├── navigation/   # Навигация
│   │   │   │   ├── screens/      # Экраны приложения
│   │   │   │   ├── theme/        # Тема (цвета, типографика)
│   │   │   │   └── viewmodel/    # ViewModel
│   │   │   ├── MainActivity.kt
│   │   │   └── MarketAnalyticsApp.kt
│   │   ├── res/
│   │   └── AndroidManifest.xml
│   └── build.gradle.kts
├── build.gradle.kts
├── settings.gradle.kts
└── README.md
```

## 🛠️ Используемые технологии

- **Kotlin** - основной язык
- **Jetpack Compose** - современный UI toolkit
- **Material 3** - дизайн-система
- **Navigation Compose** - навигация
- **Retrofit** - HTTP-клиент
- **Coroutines + Flow** - асинхронное программирование
- **DataStore** - хранение настроек
- **Hilt** - Dependency Injection

## 🎨 Дизайн

Приложение использует современную темную тему с:
- Глубоким темно-синим фоном
- Неоновыми акцентными цветами (циан, пурпурный, зеленый)
- Градиентными элементами
- Плавными анимациями переходов
- Адаптивными карточками

## 📡 API Endpoints

Приложение взаимодействует со следующими endpoints:

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | /health | Проверка состояния сервера |
| GET | /products | Получение списка товаров |
| POST | /products | Создание нового товара |
| GET | /products/{id} | Получение товара по ID |
| DELETE | /products/{id} | Удаление товара |
| POST | /products/{id}/parse | Запуск парсинга |
| GET | /products/{id}/reviews | Получение отзывов |

Все запросы требуют заголовок `X-User-Id`.

## ⚠️ Известные ограничения

1. Парсинг может занимать длительное время (до 2 минут)
2. Для работы с реальным устройством требуется настройка сети
3. Cleartext traffic разрешен для локальной разработки

## 📝 Лицензия

MIT License








