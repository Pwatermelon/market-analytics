# Market Analytics - B2B Решение для Анализа Отзывов

Микросервисное веб-приложение для парсинга, анализа и визуализации отзывов с маркетплейсов.

## 🏗️ Архитектура

Приложение состоит из следующих микросервисов:

- **API Gateway** (порт 8000) - единая точка входа, маршрутизация и авторизация
- **Auth Service** (порт 8001) - управление пользователями и JWT токенами
- **Parser Service** (порт 8002) - парсинг отзывов с маркетплейсов
- **Analyzer Service** (порт 8003) - анализ отзывов (суммаризация и тональность)
- **Frontend** (порт 3000) - React приложение
- **PostgreSQL** (порт 5432) - основная база данных
- **Redis** (порт 6379) - кеширование и очереди

## 📋 Требования

- Docker и Docker Compose
- Python 3.10+ (для локальной разработки)
- Node.js 18+ (для локальной разработки фронтенда)

## 🤖 ML Модель

### Установка модели

Перед запуском приложения необходимо скачать и разместить обученные модели.

#### Автоматическая установка (рекомендуется)

Используйте готовый скрипт для скачивания моделей:

```bash
# Установите зависимости
pip install -r requirements.txt

# Запустите скрипт с рекомендациями
python download_models_recommended.py
```

Скрипт предложит выбрать:
1. **РЕКОМЕНДУЕМЫЕ модели** (лучшее качество, ~1 GB):
   - `blanchefort/rubert-base-cased-sentiment` для тональности (~500 MB)
   - `cointegrated/rut5-base-summarizer` для суммаризации (~500 MB)

2. **ЛЕГКИЕ модели** (быстрее, меньше размер, ~560 MB):
   - `cointegrated/rubert-tiny2` для тональности (~60 MB)
   - `cointegrated/rut5-base-summarizer` для суммаризации (~500 MB)

**🏆 Рекомендация:** Выберите вариант 1 (рекомендуемые) - лучшее качество для русского языка.

Подробное сравнение моделей: [MODELS_COMPARISON.md](MODELS_COMPARISON.md)

#### Ручная установка

1. Создайте папку `models` в корне проекта:
```bash
mkdir -p models/sentiment models/summarizer
```

2. Скачайте модели через Hugging Face CLI:
```bash
# Установите Hugging Face CLI
pip install huggingface_hub

# Скачайте модель тональности
huggingface-cli download blanchefort/rubert-base-cased-sentiment --local-dir models/sentiment

# Скачайте модель суммаризации
huggingface-cli download cointegrated/rut5-base-summarizer --local-dir models/summarizer
```

### Структура папок моделей

После скачивания структура должна быть следующей:
```
models/
  ├── sentiment/          # Модель для тональности
  │   ├── config.json
  │   ├── pytorch_model.bin
  │   ├── tokenizer_config.json
  │   └── ...
  └── summarizer/         # Модель для суммаризации
      ├── config.json
      ├── pytorch_model.bin
      ├── tokenizer_config.json
      └── ...
```

**Важно:** Если модели не скачаны, приложение будет использовать модели по умолчанию из Hugging Face (но это медленнее при первом запуске).

### Использование своих моделей

**Вы можете использовать ЛЮБЫЕ модели!** Код автоматически адаптируется под разные типы моделей.

1. **Локальные модели:** Просто поместите их в `models/sentiment/` и `models/summarizer/`
2. **Модели из Hugging Face:** Укажите имя модели в `docker-compose.yml` через переменные `SENTIMENT_MODEL_NAME` и `SUMMARIZER_MODEL_NAME`
3. **Дообученные модели:** Сохраните через `model.save_pretrained()` и поместите в папки моделей

Подробнее см.:
- [MODELS_SETUP.md](MODELS_SETUP.md) - установка моделей
- [MODELS_GUIDE.md](MODELS_GUIDE.md) - полное руководство по выбору и использованию моделей

## 🚀 Запуск

1. Убедитесь, что модели размещены в папке `models/`

2. Запустите все сервисы:
```bash
docker-compose up --build
```

3. Приложение будет доступно:
   - Frontend: http://localhost:3000
   - API Gateway: http://localhost:8000
   - API Docs: http://localhost:8000/docs

## 🔐 Авторизация

### Тестовый пользователь (создается автоматически)

При первом запуске автоматически создается тестовый пользователь:

- **Email:** `test@test.com`
- **Username:** `test`
- **Password:** `test123`

### Регистрация нового пользователя

1. Откройте http://localhost:3000/register
2. Заполните форму регистрации
3. Или используйте API: `POST /api/auth/register`

### Вход в систему

1. Откройте http://localhost:3000/login
2. Введите email и пароль
3. Или используйте API: `POST /api/auth/login`
4. Получите JWT токен и используйте его в заголовке `Authorization: Bearer <token>`

## 📊 Основной функционал

- Парсинг отзывов с маркетплейсов по ссылке
- Анализ тональности отзывов
- Суммаризация отзывов
- Визуализация динамики тональности на графиках
- Отслеживание нескольких товаров
- Агрегированная суммаризация по всем отзывам товара

## 🛠️ Разработка

### Локальная разработка сервисов

Каждый сервис можно запускать отдельно:

```bash
cd services/auth-service
pip install -r requirements.txt
uvicorn main:app --reload --port 8001
```

### Локальная разработка фронтенда

```bash
cd frontend
npm install
npm start
```

## 📁 Структура проекта

```
market-analytics/
├── services/
│   ├── api-gateway/
│   ├── auth-service/
│   ├── parser-service/
│   └── analyzer-service/
├── frontend/
├── models/              # ML модели (создать вручную)
├── docker-compose.yml
└── README.md
```

## 🔧 Конфигурация

Все настройки можно изменить через переменные окружения в `docker-compose.yml`:
- JWT секреты
- Пароли БД
- URL сервисов
- Пути к моделям

## 📝 API Endpoints

- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/products` - Добавить товар для отслеживания
- `GET /api/products` - Список товаров
- `POST /api/products/{product_id}/parse` - Запустить парсинг
- `GET /api/products/{product_id}/reviews` - Получить отзывы
- `GET /api/products/{product_id}/analytics` - Аналитика по товару
- `GET /api/products/{product_id}/summary` - Суммаризация отзывов

