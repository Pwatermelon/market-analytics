# Тестовые учетные записи

## 🎯 Автоматически создаваемый тестовый пользователь

При первом запуске приложения автоматически создается тестовый пользователь:

```
Email: test@test.com
Username: test
Password: test123
```

## 🚀 Быстрый вход

### Через веб-интерфейс:

1. Откройте http://localhost:3000/login
2. Введите:
   - Email: `test@test.com`
   - Password: `test123`
3. Нажмите "Войти"

### Через API:

```bash
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@test.com",
    "password": "test123"
  }'
```

Ответ:
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer",
  "user_id": 1,
  "username": "test"
}
```

## 📝 Регистрация нового пользователя

### Через веб-интерфейс:

1. Откройте http://localhost:3000/register
2. Заполните форму:
   - Email
   - Username
   - Password
3. Нажмите "Зарегистрироваться"

### Через API:

```bash
curl -X POST http://localhost:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "username": "myuser",
    "password": "mypassword123"
  }'
```

## 🔑 Использование токена

После входа вы получите `access_token`. Используйте его для всех защищенных запросов:

```bash
curl -X GET http://localhost:8000/api/products \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

## 💡 Примечания

- Тестовый пользователь создается автоматически при первом запуске
- Если пользователь уже существует, он не будет пересоздан
- Все пароли хранятся в зашифрованном виде (bcrypt)
- Токены действительны 24 часа (настраивается через JWT_EXPIRATION)

