# Руководство по выбору и использованию ML моделей

## 🎯 Можно использовать ЛЮБЫЕ модели!

Код автоматически адаптируется под разные типы моделей. Главное - чтобы модель была в формате Hugging Face.

## 📋 Требования к моделям

### Для тональности (sentiment)

**Типы моделей, которые поддерживаются:**

1. **AutoModelForSequenceClassification** (рекомендуется)
   - Любое количество классов (2, 3, 5+)
   - Код автоматически определит структуру
   - Примеры: `blanchefort/rubert-base-cased-sentiment`, `cointegrated/rubert-tiny2`

2. **Любая модель из Hugging Face**
   - Должна поддерживать `from_pretrained()`
   - Должна возвращать `logits` при вызове

**Что нужно:**
- Модель должна быть сохранена в формате Hugging Face
- Должны быть файлы: `config.json`, `pytorch_model.bin` (или `model.safetensors`), `tokenizer_config.json`

### Для суммаризации (summarizer)

**Типы моделей, которые поддерживаются:**

1. **AutoModelForSeq2SeqLM** (рекомендуется)
   - T5, BART, mT5, и другие seq2seq модели
   - Примеры: `cointegrated/rut5-base-summarizer`, `facebook/bart-large-cnn`

2. **Любая seq2seq модель**
   - Должна поддерживать метод `.generate()`

## 🚀 Способы использования моделей

### Способ 1: Локальные модели (рекомендуется)

Поместите свои модели в папки:

```
models/
├── sentiment/
│   ├── config.json
│   ├── pytorch_model.bin
│   ├── tokenizer_config.json
│   └── ...
└── summarizer/
    ├── config.json
    ├── pytorch_model.bin
    ├── tokenizer_config.json
    └── ...
```

**Преимущества:**
- Быстрая загрузка
- Работает офлайн
- Можно использовать дообученные модели

### Способ 2: Модели из Hugging Face по имени

Укажите имя модели через переменные окружения в `docker-compose.yml`:

```yaml
analyzer-service:
  environment:
    - SENTIMENT_MODEL_NAME=your-username/your-sentiment-model
    - SUMMARIZER_MODEL_NAME=your-username/your-summarizer-model
```

**Преимущества:**
- Не нужно скачивать локально
- Автоматическое обновление
- Легко переключаться между моделями

### Способ 3: Гибридный подход

Локальная модель для тональности, Hugging Face для суммаризации (или наоборот).

## 📦 Примеры моделей, которые точно работают

### Тональность

**Русские модели:**
- `blanchefort/rubert-base-cased-sentiment` (3 класса)
- `cointegrated/rubert-tiny2` (универсальная)
- `ai-forever/ruBert-base` (можно дообучить)

**Английские модели (если нужны):**
- `cardiffnlp/twitter-roberta-base-sentiment-latest` (3 класса)
- `nlptown/bert-base-multilingual-uncased-sentiment` (5 классов)

**Свои дообученные:**
- Любая модель, сохраненная через `model.save_pretrained()`

### Суммаризация

**Русские модели:**
- `cointegrated/rut5-base-summarizer` (рекомендуется)
- `IlyaGusev/rut5_base_sum_gazeta` (для новостей)

**Английские модели:**
- `facebook/bart-large-cnn`
- `google/pegasus-xsum`

## 🔧 Как использовать свою модель

### Шаг 1: Сохранение модели

Если вы дообучили модель, сохраните её:

```python
from transformers import AutoModel, AutoTokenizer

# Ваша обученная модель
model = ...  # ваш обученный model
tokenizer = ...  # ваш tokenizer

# Сохранение
model.save_pretrained("./my_sentiment_model")
tokenizer.save_pretrained("./my_sentiment_model")
```

### Шаг 2: Размещение модели

Скопируйте папку с моделью в `models/sentiment/` или `models/summarizer/`:

```bash
cp -r ./my_sentiment_model/* models/sentiment/
```

### Шаг 3: Запуск

Просто запустите приложение - модель загрузится автоматически!

```bash
docker-compose up --build
```

## 🎨 Кастомизация обработки

Если ваша модель имеет нестандартную структуру вывода, можно изменить функцию `analyze_sentiment()` в `services/analyzer-service/main.py`.

Например, если модель возвращает только один класс (регрессия):

```python
# В analyze_sentiment() уже есть обработка для 1 класса
# sentiment_score будет напрямую из logits[0][0]
```

## ⚡ Производительность

**Легкие модели (быстрее, меньше памяти):**
- `cointegrated/rubert-tiny2` (~60 MB)
- Подходит для быстрого прототипирования

**Тяжелые модели (медленнее, больше памяти, лучше качество):**
- `blanchefort/rubert-base-cased-sentiment` (~500 MB)
- `cointegrated/rut5-base-summarizer` (~500 MB)

**Рекомендации:**
- Для продакшена: используйте локальные модели (быстрее загрузка)
- Для разработки: можно использовать модели из Hugging Face
- Для дообучения: используйте базовые модели и дообучайте на своих данных

## 🐛 Troubleshooting

### Модель не загружается

1. Проверьте структуру файлов:
```bash
ls -la models/sentiment/
# Должны быть: config.json, pytorch_model.bin, tokenizer_config.json
```

2. Проверьте логи:
```bash
docker-compose logs analyzer-service
```

3. Убедитесь, что модель совместима с transformers:
```python
from transformers import AutoModel
model = AutoModel.from_pretrained("path/to/model")  # Должно работать
```

### Неправильные результаты

1. Проверьте, что модель обучена на русском языке (если анализируете русские отзывы)
2. Проверьте количество классов модели (должно соответствовать вашим данным)
3. Попробуйте другую модель для сравнения

### Нехватка памяти

1. Используйте более легкие модели
2. Увеличьте память для Docker:
```yaml
analyzer-service:
  deploy:
    resources:
      limits:
        memory: 4G  # Увеличьте если нужно
```

## 📚 Полезные ссылки

- [Hugging Face Models](https://huggingface.co/models)
- [Transformers Documentation](https://huggingface.co/docs/transformers)
- [Russian NLP Models](https://huggingface.co/models?language=ru)

## 💡 Советы

1. **Начните с готовых моделей** - они уже обучены и работают из коробки
2. **Тестируйте на своих данных** - разные модели могут работать по-разному
3. **Дообучайте при необходимости** - можно улучшить качество на ваших данных
4. **Кешируйте результаты** - анализ можно сохранять в БД, чтобы не пересчитывать

---

**Итог:** Используйте любые модели, которые вам нравятся! Код адаптируется автоматически. 🚀

